import React from "react";
import { View, Text } from "react-native";

const SearchAnimeScreen = () => {
  return (
    <View className="flex-col pt-10 h-full bg-black">
      <Text className="text-white text-2xl">Estás en SEARCH ANIME QUESO</Text>
    </View>
  );
};

export default SearchAnimeScreen;
